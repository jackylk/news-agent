PORT=3000

# PostgreSQL数据库连接
# 支持多种平台的环境变量格式：
#
# 方式1: 使用 DATABASE_URL（推荐，Neon、Railway 等平台）
# Neon 连接字符串示例:
# DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
# Railway PostgreSQL 会自动设置 DATABASE_URL
#
# 方式2: Zeabur PostgreSQL（自动注入，无需手动配置）
# Zeabur 会自动注入以下变量，应用会自动识别：
# POSTGRES_HOST=xxx
# POSTGRES_PORT=5432
# POSTGRES_DATABASE=xxx
# POSTGRES_USERNAME=xxx
# POSTGRES_PASSWORD=xxx
#
# 方式3: 使用标准 DB_* 变量（本地开发或通用平台）
# DATABASE_URL=postgresql://user:password@localhost:5432/news_db
# 或
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=news_db
# DB_USER=postgres
# DB_PASSWORD=your_password
#
# 本地开发：设置 USE_LOCAL_DB=true 使用本地数据库
# USE_LOCAL_DB=true

CORS_ORIGIN=*

# AI摘要服务配置（推荐使用DeepSeek）
# DeepSeek API Key（推荐）
DEEPSEEK_API_KEY=sk

# 如果使用OpenAI，设置以下环境变量：
# OPENAI_API_KEY=your_openai_api_key

# 如果使用Hugging Face，设置以下环境变量：
# HUGGINGFACE_API_KEY=your_huggingface_api_key

# 管理员令牌（用于管理界面访问）
# 生产环境请使用强密码
ADMIN_TOKEN=admin123

# JWT Secret（用于用户认证token加密）
# 生产环境必须使用强密码
JWT_SECRET=your-secret-key-change-in-production

# Twitter/X 推文抓取配置（可选）
# 支持通过Nitter实例将Twitter用户转换为RSS源
# 可以配置多个Nitter实例作为备用（用逗号分隔）
# 如果未配置，将使用默认的公共Nitter实例（可能不稳定）
# 建议：自己部署Nitter实例以获得更好的稳定性
# NITTER_INSTANCES=https://nitter.net,https://nitter.it,https://nitter.pussthecat.org

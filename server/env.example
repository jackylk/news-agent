PORT=3000

# PostgreSQL数据库连接
# 推荐使用 Neon Serverless PostgreSQL (https://neon.tech)
# 或 Railway 内置 PostgreSQL
# 
# 方式1: 使用 DATABASE_URL (推荐，Neon 和 Railway 都支持)
# Neon 连接字符串示例:
# DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/dbname?sslmode=require
# Railway PostgreSQL 会自动设置 DATABASE_URL
#
# 方式2: 使用单独的环境变量（本地开发）
# DATABASE_URL=postgresql://user:password@localhost:5432/news_db
# 或
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=news_db
# DB_USER=postgres
# DB_PASSWORD=your_password

CORS_ORIGIN=*

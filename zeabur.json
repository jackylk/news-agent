{
  "$schema": "https://json-schema.zeabur.com/config.json",
  "build": {
    "type": "dockerfile",
    "dockerfile": "Dockerfile"
  },
  "env": {
    "PORT": {
      "default": "3000",
      "description": "服务监听端口，Zeabur 会自动注入"
    },
    "NODE_ENV": {
      "default": "production",
      "description": "运行环境"
    }
  }
}
